set -e # exit immediately if a simple command exits with a non-zero status
set -u # report the usage of uninitialized variables

# Detect # of CPUs so make jobs can be parallelized
CPUS=$(grep -c ^processor /proc/cpuinfo)
 # Available variables
# $BOSH_COMPILE_TARGET - where this package & spec'd source files are available
# $BOSH_INSTALL_TARGET - where you copy/install files to be included in package
# uncomment export BOSH_INSTALL_TARGET for local testing
# export BOSH_INSTALL_TARGET=$PWD/install
# comment the export HOME line for local testing
export HOME=/var/vcap
# export HOME=$PWD
export PKG_CONFIG_PATH=${BOSH_INSTALL_TARGET}/lib/pkgconfig
export LD_LIBRARY_PATH=${BOSH_INSTALL_TARGET}/lib
export INCLUDE_PATH=${BOSH_INSTALL_TARGET}/include
export LDFLAGS=-L${LD_LIBRARY_PATH}
export CPPFLAGS=-I${INCLUDE_PATH}
export PIP_ROOT_USER_ACTION=ignore
export PATH="${BOSH_INSTALL_TARGET}/bin:${BOSH_INSTALL_TARGET}/sbin:${PATH}"

# Build libffi 3.4.6
# https://github.com/libffi/libffi/releases/download/v3.4.6/libffi-3.4.6.tar.gz
tar -xzf tshark/libffi-3.4.6.tar.gz
(cd libffi-3.4.6
 ./configure --prefix ${BOSH_INSTALL_TARGET} && \
 make -j${CPUS} && \
 make install) || exit 1

tar -xzf python/Python-3.14.2.tgz
(cd Python-3.14.2
  export C_INCLUDE_PATH="${BOSH_INSTALL_TARGET}/include"
  export LIBRARY_PATH="${BOSH_INSTALL_TARGET}/lib"
  export LD_LIBRARY_PATH="${BOSH_INSTALL_TARGET}/lib"
 ./configure --prefix ${BOSH_INSTALL_TARGET} && \
 make -j${CPUS} && \
 make install) || exit 1

# Have python use BOSH_INSTALL_TARGET instead of $HOME/.local/ for installing python packages
export PYTHONUSERBASE="${BOSH_INSTALL_TARGET}"

# Install meson build system
# download https://github.com/mesonbuild/meson/releases/tag/1.10.1
pip3 install --root-user-action=ignore --user tshark/meson-1.10.1.tar.gz

# Install ninja build system for meson
# Rename the blob to contain the version number in the filename (ninja-1.13.2-linux.zip)
# download https://github.com/ninja-build/ninja/releases/download/v1.13.2/ninja-linux.zip

unzip tshark/ninja-1.13.2-linux.zip
mv ninja ${BOSH_INSTALL_TARGET}/bin

# Build glib 2.82.2 - requires meson build system
# https://download.gnome.org/sources/glib/2.82/glib-2.82.2.tar.xz
tar -xJf tshark/glib-2.82.2.tar.xz
(cd glib-2.82.2/
 # glib 2.82+ requires meson/ninja, fall back to older version if not available
 if command -v meson >/dev/null 2>&1; then
   meson setup builddir --verbose --prefix=${BOSH_INSTALL_TARGET} && \
   meson compile -C builddir --verbose -j${CPUS} && \
   meson install -C builddir --verbose
 else
   # If meson not available, this will fail - glib 2.82+ requires meson
   echo "ERROR: glib 2.82+ requires meson build system"
   exit 1
 fi
) || (echo $?; exit 1)

# Build speexdsp 1.2.1
# https://github.com/xiph/speexdsp/releases/tag/SpeexDSP-1.2.1
tar -xzf tshark/speexdsp-SpeexDSP-1.2.1.tar.gz
(cd speexdsp-SpeexDSP-1.2.1
 ./autogen.sh
 ./configure --prefix ${BOSH_INSTALL_TARGET} && \
 make -j${CPUS} && \
 make install) || exit 1

# Build c-ares 1.34.6
# https://c-ares.org/download/
tar -xzf tshark/c-ares-1.34.6.tar.gz
(cd c-ares-1.34.6
 ./configure --prefix ${BOSH_INSTALL_TARGET} && \
 make -j${CPUS} && \
 make install) || exit 1

# Build libpcap 1.10.5
# https://www.tcpdump.org/release/libpcap-1.10.5.tar.gz
tar -xzf tshark/libpcap-1.10.5.tar.gz
(cd libpcap-1.10.5
 ./configure --prefix ${BOSH_INSTALL_TARGET} && \
 make -j${CPUS} && \
 make install) || exit 1

# Build libgpg_error 1.58
# https://gnupg.org/ftp/gcrypt/libgpg-error/
tar -xjf tshark/libgpg-error-1.58.tar.bz2
(cd libgpg-error-1.58
 ./configure --prefix ${BOSH_INSTALL_TARGET} && \
 make -j${CPUS} && \
 make install) || exit 1

# Build libgcrypt 1.11.2
# https://gnupg.org/ftp/gcrypt/libgcrypt/
tar -xjf tshark/libgcrypt-1.11.2.tar.bz2
(cd libgcrypt-1.11.2
 ./configure --prefix ${BOSH_INSTALL_TARGET} && \
 make -j${CPUS} && \
 make install) || exit 1

# Build Wireshark 4.4.2 - uses cmake instead of autotools
# https://www.wireshark.org/download/src/all-versions/wireshark-4.4.2.tar.xz
# -DENABLE_PCAP=OFF

tar -xJf tshark/wireshark-4.4.2.tar.xz
(cd wireshark-4.4.2
 # Wireshark 4.x requires cmake
 if command -v cmake >/dev/null 2>&1; then
   export C_INCLUDE_PATH="${BOSH_INSTALL_TARGET}/include"
   mkdir -p build && cd build
   cmake .. \
     -DCMAKE_INSTALL_PREFIX=${BOSH_INSTALL_TARGET} \
     -DBUILD_wireshark=OFF \
     -DENABLE_APPLICATION_BUNDLE=OFF \
     -DENABLE_QT=OFF \
     -DBUILD_randpkt=OFF && \
   make -j${CPUS} tshark && \
   make install
 else
   echo "ERROR: Wireshark 4.x requires cmake build system"
   exit 1
 fi
) || exit 1
